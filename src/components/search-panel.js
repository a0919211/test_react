import React from 'react';

const SearchPanel = () => {
    return (
        <input
            placeholder={"Start typing..."}
            autoComplete="Start typing..." />
    )
};

export default SearchPanel;